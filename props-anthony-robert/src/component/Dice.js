import React, { Component } from 'react' //So 'rcc' is what generates this basic code

export default class Dice extends Component{

    render(){
        return(
            <> 
            <div className = 'square' onClick={}>
            </div>
            <h1> Click Here To Roll</h1>
            {/* <button>For Show</button> */}
            </> 
    ) //Make sure we add our frag <></> inside the () of render
    }     // Create div className = 'square' onClick={}>
          // all inside if frag <></>
}
}